Sandbox to (vm) before updating to GitHub/      changing Version #'s all the way around.


insert a banner:        then remove before update
      snap a the current version for a resave state  before applying new changes
 
       every new state gets a new snap to the patch or update




      
.\gradlew.bat assembleRelease   -apk update timestamp
.\gradlew.bat bundleRelease     -abb update timestamp.

git add .
git commit -m "Release v7.27.15 - Compass Bug Fixes"
git push
